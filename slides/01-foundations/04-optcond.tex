\documentclass[11pt,compress,t,notes=noshow, xcolor=table]{beamer}

\input{../../style/preamble}
\input{../../latex-math/basic-math}
\input{../../latex-math/optim-basics}

\title{Optimization in Machine Learning}

\begin{document}

\titlemeta{
Foundations
}{
Conditions for optimality
}{
figure_man/local_global_min_2D.png
}{
\item Local and global optima
\item Strict, isolated, and unique minimizers
\item First \& second order conditions
}

\begin{framei}{Extrema and saddle points}
\item Given $\S \subseteq \R^d$, $f: \S \to \R$
\item Global minimum at $\xvs$: $\fxvs \le \fx$ for all $\xv \in \S$
\item Local minimum at $\xvs$: $\exists\, \eps > 0$ s.t. $\fxvs \le \fx$ for all $\xv \in \S \cap B_\eps(\xvs)$ ($\eps$-ball)
\item Analogously for global and local max
\item We call $\xvs$ saddle point if in feasible portion of every eps-ball
$\S \cap B_\eps(\xvs)$, is at least a strictly better and a strictly worse point
\vfill
\splitV[0.5]{
\imageC[0.9][https://en.wikipedia.org/wiki/Maxima_and_minima]{figure_man/local_global_min.png}
}{
\imageC[0.9][https://wngaw.github.io/linear-regression/]{figure_man/local_global_min_2D.png}
}
\comment[NOTE]{BB}{sagen dass lokal nur bei innerem punkt definiert ist, sagen dass max analog ist; wir schneiden hier mit dem eps ball}
\end{framei}

\begin{framei}{Variants of minimizers}
\item Several refined notions for $\xvs \in \S$:
\item[\textbf{Strict local min:}] $\fxvs < \fx$ for all $\xv \in \S \cap B_\eps(\xvs)$ with $\xv \neq \xvs$
\item[\textbf{Isolated local min:}] $\xvs$ is a local min and $B_\eps(\xvs)$ contains no other local min
\item[\textbf{Unique global min:}] $\xvs$ is the only global minimizer on $\S$
\spacer
\item Hierarchy: unique global $\Rightarrow$ isolated local $\Rightarrow$ strict local $\Rightarrow$ local
\item Reverse implications generally fail
\item \textbf{Caveat:} strict local does NOT imply isolated -- example $f(x) = x^4 (2 + \sin(1/x))$ at $x=0$
\end{framei}


\begin{framei}{Existence of optima}
\item $f: \S \to \R$
\item If $f$ continuous and $\S$ compact: minimum and maximum exist (extreme value theorem)
\item If $f$ discontinuous: no general existence statement
\item Negative example, with $\S = [0,1]$:
%FIXME: here some plots for the functions below are missing
\begin{align*}
f(x) = \begin{cases}
1/x  & x > 0 \\
0    & x = 0 \\
-1/x & x < 0
\end{cases}
\end{align*}
\end{framei}


\begin{framei}{First-order condition}
\item Let $f: \S \to \R$, $f$ differentiable, $\xvs$ interior point of $\S$
\item Necessary condition:\\
If $\xvs$ is a local extremum, then $\nabla \fxvs = 0$
\item Such points are called ``stationary''
\item Intuition: at a local extremum, the function must be flat, otherwise we can find a direction to move to a better value
\item Not sufficient, e.g. saddle points are possible
\vfill
\imageC[0.6][https://github.com/jermwatt/machine_learning_refined]{figure_man/first_order.png}
\end{framei}

\begin{framei}[fs=footnotesize]{Second-order condition}
\item Let $f: \S \to \R$, $f \in \CC{2}$, $\xvs$ interior point of $\S$
\item If $H(\xvs)$ is definite, then $\xvs$ is a strict local extremum
\item If $H(\xvs)$ is semi-definite, then $\xvs$ is a local extremum
\item If $H(\xvs)$ is indefinite, then $\xvs$ is a saddle point
\item If $H(\xvs)$ is p(s)d, then $\xvs$ is a (strict) local min\\
this implies $f$ is locally (strictly) convex
\item If $H(\xvs)$ is n(s)d, then $\xvs$ is a (strict) local max\\
this implies $f$ is locally (strictly) concave
\item Interpretation: curvature pos (or neg) in all directions
\imageC[0.7]{figure_man/convex.jpg}
\end{framei}


\begin{framei}{Example: Branin function}
\item Branin function with 3 local minima
\splitV{
\imageC[0.65]{figure/branin3d/branin2D.pdf}
}{
\imageC[0.65]{figure/branin3d/branin3D.pdf}
}
\item EVs of Hessian at local minima:
\begin{center}
\begin{tabular}{c|c|c}
        & $\lambda_1$ & $\lambda_2$ \\ \hline\hline
Left    & 22.29       & 0.96        \\ \hline
Middle  & 11.07       & 1.73        \\ \hline
Right   & 11.33       & 1.69        \\
\end{tabular}
\end{center}
\end{framei}

\begin{framei}{Convexity and optima}
\item $f: \S \to \R$ convex on convex set $\S$
\item Any local minimum is global
\item The set of minima is convex
\item If $f$ strictly convex: at most one local minimum\\
(unique global on $\S$, if it exists)
\item Analogously for concave functions
\end{framei}


\begin{framei}[fs=footnotesize]{Convexity and optima: proofs}
\item[\textbf{Local = global.}] Assume $\xvs$ local min but not global, so $\exists \bar{\xv} \in \S$ with $f(\bar{\xv}) < f(\xvs)$. By convexity, $\forall t \in [0,1]$:
$$
f(\xvs + t(\bar{\xv} - \xvs)) \leq (1-t) f(\xvs) + t f(\bar{\xv}) < f(\xvs)
$$
For $t$ small enough, the point $\xvs + t(\bar{\xv} - \xvs) \in B_\eps(\xvs) \cap \S$ -- contradiction.
\spacer
\item[\textbf{Set of minima convex.}] Let $\xvs, \bar{\xv}$ both be global mins, so $f(\xvs) = f(\bar{\xv}) = f^\ast$. By convexity, $\forall t \in [0,1]$:
$$
f(\xvs + t(\bar{\xv} - \xvs)) \leq (1-t) f^\ast + t f^\ast = f^\ast
$$
Since $f^\ast$ is the global min, equality must hold, so $\xvs + t(\bar{\xv} - \xvs)$ is also a global minimizer.
\spacer
\item[\textbf{Strict convex $\Rightarrow$ at most one min.}] Same setup with $\xvs \neq \bar{\xv}$, both global mins. Strict convexity gives \textbf{strict} inequality $f(\xvs + t(\bar{\xv} - \xvs)) < f^\ast$ for $t \in (0,1)$ -- contradiction.
\end{framei}


\begin{framei}{Example}
\item $f(x,y) = x^4 + y^4 - x^2 - y^2$
\item $\nabla f(x,y) = (4x^3 - 2x, 4y^3 - 2y)$
\item $H(x,y) = \mat{
12x^2 - 2 &  0          \\
0         & 12y^2 - 2
}$
\item At $(0,0)^T$ we have strict local max
\item At $(\pm \frac1{\sqrt{2}}, \pm \frac1{\sqrt{2}})^T$ we have 4 strict local min
\item At $(0, \pm \frac1{\sqrt{2}})^T$, $(\pm \frac1{\sqrt{2}}, 0)^T$ we have 4 saddle points
\comment[FIX]{BB}{hier fehlt ein plot, oder vielleicht ein zweites bsp mit einer stetigen funktion --> folie umschreiben}
\end{framei}

\endlecture
\end{document}
